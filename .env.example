# .env.example
SECRET_KEY=unsafe-local-secret-change-me
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost

# Either use DATABASE_URL or the per-value fields below.
# Example using DATABASE_URL for a local MySQL server:
DATABASE_URL=mysql://myuser:mypassword@127.0.0.1:3306/jwtproj_db

# Or per-value (un-comment & edit if you prefer per-value)
# DATABASE_ENGINE=django.db.backends.mysql
# DATABASE_NAME=jwtproj_db
# DATABASE_USER=myuser
# DATABASE_PASSWORD=mypassword
# DATABASE_HOST=127.0.0.1
# DATABASE_PORT=3306

PORT=8000
